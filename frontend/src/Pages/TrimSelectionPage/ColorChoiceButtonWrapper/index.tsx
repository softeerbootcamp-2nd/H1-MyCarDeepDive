import { WrapperProps } from '@/global/type';

function ColorChoiceButtonWrapper({ children }: WrapperProps) {
  return <div className='mt-6'>{children}</div>;
}

export default ColorChoiceButtonWrapper;
