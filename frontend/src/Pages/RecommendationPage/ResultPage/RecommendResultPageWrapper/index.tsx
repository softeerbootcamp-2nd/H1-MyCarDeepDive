import { WrapperProps } from '@/global/type';

function RecommendResultPageWrapper({ children }: WrapperProps) {
  return <div className='pt-[92px]'>{children}</div>;
}

export default RecommendResultPageWrapper;
